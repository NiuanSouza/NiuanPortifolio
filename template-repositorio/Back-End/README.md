# Back-End

> Explicação focada no servidor, banco de dados e regras de negócio.

## ⚙️ Arquitetura e Modelagem
Explique como o banco de dados foi modelado e a arquitetura do servidor (ex: MVC, Clean Architecture).

## 🛠 Tecnologias Usadas
- Node.js (Exemplo)
- Express (Exemplo)
- PostgreSQL (Exemplo)

## 🚀 Como Rodar o Back-End
1. Configure as variáveis de ambiente baseadas no `.env.example`.
2. `npm install`
3. `npm run start:dev`
